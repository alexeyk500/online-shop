export type TypeType = {
  id: string;
  name: string;
};

export type BrandType = {
  id: string;
  name: string;
};

export type DeviceType = {
  id: string;
  name: string;
  price: string;
  img: string;
  rating: string;
  typeId: string;
  brandId: string;
};
